import { StyleSheet } from 'react-native';
import { colors, fonts } from '../../../style';

const styles = StyleSheet.create({
  text: {
    fontFamily: fonts.font.StarJedi,
    color: colors.white,
  },
});

export default styles;
